[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# The CMS admin is a static page inside the publish dir — it must be matched
# before the SPA catch-all below, or React Router would swallow /admin.
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200

# Client-side routing: every other path renders the React app.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
